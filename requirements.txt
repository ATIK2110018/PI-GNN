# ── Core deep learning (GPU) ──────────────────────────────────
# Environment: torch310 conda env
# GPU: NVIDIA GeForce RTX 2050 | CUDA 12.1 | PyTorch 2.5.1+cu121
torch>=2.5.1          # installed: 2.5.1+cu121
torch-geometric>=2.8  # installed: 2.8.0
torch-scatter         # required by torch-geometric
torch-sparse          # required by torch-geometric

# ── Scientific computing ──────────────────────────────────────
numpy>=1.24
scipy>=1.13
pandas>=2.2

# ── Geospatial / HEC-RAS ─────────────────────────────────────
h5py>=3.11            # HEC-RAS HDF5 I/O
pyproj>=3.7           # lat/lon → UTM coordinate conversion
rasterio>=1.4         # terrain TIF sampling
geopandas
shapely
pyshp

# ── Visualization ─────────────────────────────────────────────
matplotlib>=3.8

# ── Utilities ─────────────────────────────────────────────────
pyyaml
tqdm
scikit-learn
networkx

# ── Run command ───────────────────────────────────────────────
# conda run -n torch310 python main.py --device cuda
