# ── Kaggle-optimized requirements ─────────────────────────────
# Kaggle T4 already has: torch, numpy, scipy, pandas, matplotlib,
# h5py, scikit-learn, networkx, pyyaml, tqdm, shapely, geopandas
# Only install what is MISSING on Kaggle:

torch-geometric>=2.8   # GNN framework — NOT pre-installed on Kaggle
pyproj>=3.7            # UTM→WGS84 reprojection for LULC sampling
rasterio>=1.4          # terrain + LULC GeoTIFF sampling
